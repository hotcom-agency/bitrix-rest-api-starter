limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /var/www/html;
    index index.php;

    access_log /var/log/nginx/access.log combined;
    error_log /var/log/nginx/error.log warn;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # CORS (Prod: без отладочных заголовков)
    add_header 'Access-Control-Allow-Origin' $http_origin always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, PATCH, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With, Accept' always;
    add_header 'Access-Control-Max-Age' 86400 always;
    add_header 'Vary' 'Origin' always;

    # Gzip (Prod: максимальное сжатие)
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_types application/json application/javascript text/css text/xml application/xml image/svg+xml;

    client_max_body_size 64m;
    
    # OPTIONS preflight
    error_page 405 =204 @empty_response;
    location @empty_response {
        add_header 'Content-Type' 'text/plain charset=UTF-8';
        add_header 'Content-Length' 0;
        return 204;
    }

    # 1. Блокировка системных папок
    location ~* ^/bitrix/(cache|sessions|tmp|stack_cache|managed_cache)/ {
        deny all; return 404;
    }

    # 2. Запрет исполнения PHP в служебных директориях
    location ~ ^/(upload|vendor|config|local/logs|docker)/.*\.(php|phtml|php5|php7|php8|phar|pl|py|jsp|asp|sh|cgi)$ {
        deny all; return 403;
    }

    # 3. PHP в /bitrix/ (админка, модули, ядро)
    # Строго ДО общего location ~ \.php$
    location ~* /bitrix.+\.php$ {
        try_files $uri @bitrixadm;
        limit_except GET POST PUT DELETE PATCH OPTIONS { deny all; }
        limit_req zone=api_limit burst=50 delay=15;
        limit_req_status 429;
        limit_req_log_level warn;

        fastcgi_pass php:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param SCRIPT_NAME $fastcgi_script_name;
        fastcgi_param REQUEST_URI $request_uri;
        include fastcgi_params;

        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_connect_timeout 60s;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 8 16k;
        fastcgi_busy_buffers_size 24k;
    }

    # 4. Все остальные PHP-файлы
    location ~ \.php$ {
        try_files $uri @bitrix;
        limit_except GET POST PUT DELETE PATCH OPTIONS { deny all; }
        limit_req zone=api_limit burst=20 nodelay;
        limit_req_status 429;
        limit_req_log_level warn;

        fastcgi_pass php:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param SCRIPT_NAME $fastcgi_script_name;
        fastcgi_param REQUEST_URI $request_uri;
        include fastcgi_params;

        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_connect_timeout 60s;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 8 16k;
        fastcgi_busy_buffers_size 24k;
    }

    # 5. Загрузки
    location /upload/ {
        limit_except GET HEAD OPTIONS { deny all; }
        expires 30d;
        add_header Cache-Control "public, immutable, no-transform";
        add_header X-Content-Type-Options "nosniff" always;
    }

    # 6. Статические ассеты
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff2?|ttf|eot|otf|webp)$ {
        limit_except GET HEAD OPTIONS { deny all; }
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
        log_not_found off;
    }

    # 7. D7 Router (включая /api/)
    location / {
        try_files $uri $uri/ @bitrix;
        limit_except GET POST PUT DELETE PATCH OPTIONS { deny all; }
        limit_req zone=api_limit burst=20 nodelay;
        limit_req_status 429;
        limit_req_log_level warn;
    }

    # 8. Точка входа D7 Router
    location @bitrix {
        fastcgi_pass php:9000;
        fastcgi_param SCRIPT_FILENAME $document_root/bitrix/routing_index.php;
        fastcgi_param SCRIPT_NAME /bitrix/routing_index.php;
        fastcgi_param REQUEST_URI $request_uri;
        fastcgi_param QUERY_STRING $query_string;
        include fastcgi_params;

        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_connect_timeout 60s;
    }

    # 9. Fallback для админки
    location @bitrixadm {
        fastcgi_pass php:9000;
        fastcgi_param SCRIPT_FILENAME $document_root/bitrix/admin/404.php;
        fastcgi_param SCRIPT_NAME /bitrix/admin/404.php;
        fastcgi_param REQUEST_URI $request_uri;
        include fastcgi_params;

        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_connect_timeout 60s;
    }
}
